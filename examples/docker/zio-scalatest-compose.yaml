services:
  gateway:
    image: local/eak-gateway-core:latest
    restart: always
    #    ports:
    #      - "8080:8080"
    #      - "8081:8081"
    environment:
      DATABASE_NAME: local_db
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_USERNAME: local_user
      DATABASE_PASSWORD: local_password